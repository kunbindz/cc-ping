// uninstall.mjs — remove ONLY cc-ping's hook entries from ~/.claude/settings.json.
// STATUS: STUB — Phase 6 (Codex). Must leave all other user hooks intact, and leave
// settings.json valid. Idempotent (running when nothing is installed is a no-op).
process.stdout.write('uninstall.mjs not yet implemented — see CODEX_BRIEF.md Phase 6\n');
