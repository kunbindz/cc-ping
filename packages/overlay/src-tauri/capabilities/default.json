{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Frontend only listens for the 'cc-ping-event'. Window show/hide/position and all sound are driven by Rust (see server.rs / lib.rs), so no window permissions are needed here.",
  "windows": ["mascot"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
